apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bdc-ingest.fullname" . }}-test-connection"
  labels:
    {{- include "bdc-ingest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bdc-ingest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
