apiVersion: v1
kind: Service
metadata:
  name: {{ include "bdc-ingest.fullname" . }}
  labels:
    {{- include "bdc-ingest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bdc-ingest.selectorLabels" . | nindent 4 }}
