# CLI argument parsing
click

# HTTP(S) requests
requests

# LakeFS filesystem access
lakefs-spec~=0.11.1

# Reusable Dug data model classes (DugStudy, DugVariable, DugSection, ...)
# Not yet published to PyPI -- install from GitHub. Requires Python >= 3.12.
git+https://github.com/helxplatform/dug-data-model.git@acdc601c4b1702858eb08c6313bf11e42de546ea
